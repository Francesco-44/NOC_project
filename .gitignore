# Colcon artifacts
/build/
/install/
/log/
/bags_recordings/

# Python caches
__pycache__/
*.py[cod]

# Tooling caches
.pytest_cache/
.mypy_cache/

# Editor / OS
.vscode/
.DS_Store

# Tuning artifacts
tuning_results/**/rosbag/
*.db3

# Large source videos kept locally only (GIF preview is committed instead)
assets/*.mp4

# Figures, metrics and rosbags produced by the offline tools: all of them are
# regenerated by metrics/make_results.py, so they are not versioned.
/metrics/out/
/metrics/bags/

# LaTeX build tree, if the report is compiled inside the repo
/report/
